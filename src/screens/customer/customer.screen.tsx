import { FC } from 'react';
import { AppStackScreenProps } from 'src/navigators/main.stack';

type FCProps = AppStackScreenProps<'CUSTOMER'>;

export const CustomerScreen: FC<FCProps> = () => {
  return <></>;
};
